(ns clj-jms-activemq-fifo-writer.core-test
  (:require [clojure.test :refer :all]
            [clj-jms-activemq-fifo-writer.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
